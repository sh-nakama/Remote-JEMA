node_modules
dist
dist-ssr
*.log
.DS_Store
.vite

# generated web-data snapshots (produced by `repower export-web`)
public/data/

# Old tsc -b outputs: the configs no longer emit, and the npm scripts pass --config
# vite.config.ts so a leftover vite.config.js can't shadow it.
*.tsbuildinfo
vite.config.js
vite.config.d.ts
